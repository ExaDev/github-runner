{# Values for the gha-runner-scale-set-controller chart, rendered on the control node by install_controller.yml. With every option at its default this renders to an empty document, so the chart's own defaults apply: one controller, watching every namespace, which serves every org's scale sets. #}
